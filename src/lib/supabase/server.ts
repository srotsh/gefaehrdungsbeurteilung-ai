// Shim für generierte Routen (gen-lead-drip, gen-lead-magnet, gen-unsubscribe),
// die "@/lib/supabase/server" importieren. Kanonische Clients: @flow/db.
export { createServerClient, createServiceClient } from "@flow/db";
